import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(

  // the routes will be changed as we progress in the work

  initialRoute: '/',
  routes: {
    // '/': (context) => Loading(),
    // '/home': (context) => Home(),
    // '/settings': (context) => Settings(),

  }
));


